<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorAccent">@color/accent</item>
        <item name="colorPrimary">@color/primaryDark</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="toolbarStyle">@style/AppTheme.Toolbar</item>
        <item name="colorControlNormal">@color/icon</item>
        <item name="android:textColorPrimary">@color/textDark</item>
        <item name="android:textColorSecondary">@color/textLight</item>
    </style>

    <style name="AppThemeWithActionBar" parent="@android:style/Theme.DeviceDefault.Light.DarkActionBar">
        <item name="colorAccent">@color/accent</item>
        <item name="colorPrimary">@color/primaryDark</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorControlNormal">@color/icon</item>
        <item name="android:textColorPrimary">@color/textDark</item>
        <item name="android:textColorSecondary">@color/textLight</item>
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">
            ?colorPrimaryDark
        </item>
    </style>

    <style name="AppTheme.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="popupTheme">@style/AppTheme.Toolbar.Popup</item>
        <item name="titleTextAppearance">@style/AppTheme.Toolbar.Title</item>
    </style>

    <style name="AppTheme.Toolbar.Title" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/icon</item>
    </style>

    <style name="AppTheme.Toolbar.Popup" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/primaryLight</item>
        <item name="android:textColorPrimary">@color/textDark</item>
        <item name="android:textColorSecondary">@color/textLight</item>
    </style>

    <style name="AppTheme.ActionBar" parent="@android:style/Widget.DeviceDefault.Light.ActionBar">
        <item name="android:background">@color/primary</item>
        <item name="android:titleTextStyle">@style/AppTheme.Toolbar.Title</item>
    </style>

</resources>
